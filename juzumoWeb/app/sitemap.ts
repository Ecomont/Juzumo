import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Sitemap',
  description: 'Sitemap for SEO purposes',
};

const Sitemap = () => {
  return null; // This component is intentionally left blank as the sitemap is generated dynamically.
};

export default Sitemap;